<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <%= link_to 'Notes App', root_path, class: 'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto align-items-center">
        <% if logged_in? %>
        <li class="nav-item">
          <%= link_to 'User Dashboard', user_path(current_user), class: "nav-link" %>
        </li>
          <div class="mx-3 nav-link">
            <%= link_to 'Log out', logout_path, method: :delete, class: "btn btn-primary btn-sm" %>
          </div>
        <% else %>
          <li><%= link_to 'Log in', login_path, class: 'btn btn-primary btn-sm' %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>